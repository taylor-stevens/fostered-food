/**
 * Tests for the frontend utils.
 */
describe('utils', () => {
    describe('getFridgeOrder', () => {
        it('correctly orders the Fridges with the name as the input field', () => {
            // TODO: write this test.
        });
        it('correctly orders the Fridges with the address as the input field', () => {
            // TODO: write this test.
        });
        it('correctly orders the Fridges with the lastOpen as the input field', () => {
            // TODO: write this test.
        });
        it('correctly orders the Fridges with the temperature as the input field', () => {
            // TODO: write this test.
        });
        it('correctly orders the Fridges with the distance as the input field', () => {
            // TODO: write this test.
        });
    });
    describe('sortByDistanceToFridge', () => {
        it('does not throw an error when the fridges input is empty', () => {
            // TODO: write this test.
        });
        it('correctly orders the fridge objects by their distance field', () => {
            // TODO: write this test.
        });
    });
    describe('setDistanceFromUser', () => {
        it('does not throw an error when the fridges input is empty', () => {
            // TODO: write this test.
        });
        it('sets the distance field for each fridge in the list', () => {
            // TODO: write this test.
        });
    });
    describe('dateTimeToReadable', () => {
        it('replaces the Fridges lastOpen field with a readable string version', () => {
            // TODO: write this test.
        });
    });
    describe('containsExplicitText', () => {
        it('returns true if the string contains explicit text', () => {
            // TODO: write this test.
        });
        it('returns false if the string does NOT contain explicit text', () => {
            // TODO: write this test.
        });
    });
    describe('todaysDateShortened', () => {
        it('returns a shortened version of todays Date', () => {
            // TODO: write this test.
        });
    });
});