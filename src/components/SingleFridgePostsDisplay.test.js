/**
 * Tests for the SingleFridgePostsDisplay
 */
describe('SingleFridgePostsDisplay', () => {
    describe('always renders', () => {
        it('the singleFridgePostsDisplay', () => {
            // TODO: write this test
        });
    });
    describe('when the number of posts is zero', () => {
        it('renders the singleFridgePostsDisplay', () => {
            // TODO: write this test
        });
        it('does NOT render the singleFridgePostItem', () => {
            // TODO: write this test
        })
    });
    describe('when the number of posts is NOT zero (greater than 0)', () => {
        it('renders the singleFridgePostsDisplay', () => {
            // TODO: write this test
        });
        it('renders the same number of singleFridgePostItems as there are posts', () => {
            // TODO: write this test
        })
    });
});