/**
 * Tests for the UserNotification component
 */
describe('UserNotification', () => {
    describe('always renders', () => {
        it('the userNotificationPopup', () => {
            // TODO: write this test
        });
    });
    describe('when the close button is rendered', () => {
        it('renders the userNotificationPopup', () => {
            // TODO: write this test
        });
        it('renders the userNotificationCloseButton', () => {
            // TODO: write this test
        });
    });
    describe('when the close button is NOT rendered', () => {
        it('renders the userNotificationPopup', () => {
            // TODO: write this test
        });
        it('does not render the userNotificationCloseButton', () => {
            // TODO: write this test
        });
    });
});