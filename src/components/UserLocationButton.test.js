/**
 * Tests for the User Location Button.
 */
describe('UserLocationButton', () => {
    describe('whether or not user location is known (NOT loading)', () => {
        it('renders userLocationButton', () => {
            // TODO: write this test.
        });
        it('renders the locationButtonSymbol', () => {
            // TODO: write this test.
        });
        it('does not render the locationLoadingSymbol', () => {
            // TODO: write this test.
        });
    });
    describe('when user location button is clicked', () => {
        it('renders the locationLoadingSymbol', () => {
            // TODO: write this test.
        });
        it('does NOT render the locationButtonSymbol', () => {
            // TODO: write this test.
        });
        it('does NOT render the userLocationMarker', () => {
            // TODO: write this test.
        });
    });
    describe('when user location is unknown', () => {
        it('does NOT render the userLocationMarker', () => {
            // TODO: write this test.
        });
    });
    describe('when user location is known', () => {
        it('renders the userLocationMarker', () => {
            // TODO: write this test.
        });
    });
});