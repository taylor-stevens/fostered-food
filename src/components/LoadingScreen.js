import React from "react";

/**
 * Creates an element to default to when the fridge data cannot be retrieved.
 * @returns {JSX.Element} An element that will fill the space when data cannot load.
 */
const LoadingScreen = () => {
    return <div>Loading</div>
}

export default LoadingScreen