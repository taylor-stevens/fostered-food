/**
 * Tests for the SingleFridgeInfoDisplay
 */
describe('SingleFridgeInfoDisplay', () => {
   describe('always renders', () => {
       it('the singleFridgeInfoDisplay', () => {
           // TODO: write this test
       });
       it('the fridgeLastVisit', () => {
           // TODO: write this test
       });
       it('the fridgeCurrentTemperature', () => {
           // TODO: write this test
       });
       it('the singleFridgePostForm', () => {
           // TODO: write this test
       });
       it('the singleFridgePostsDisplay', () => {
           // TODO: write this test
       });
   });
});