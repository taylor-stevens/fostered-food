/**
 * Tests for the SingleFridgeLocationMarker.
 */
describe('SingleFridgeLocationMarker', () => {
    describe('always renders', () => {
        it('the singleFridgeLocationMarker', () => {
            // TODO: write this test
        });
    });
    describe('when the marker is NOT clicked', () => {
        it('renders the singleFridgeLocationMarker', () => {
            // TODO: write this test
        });
        it('does NOT render the singleFridgeLocationPopup', () => {
            // TODO: write this test
        });
    });
    describe('when the marker is clicked', () => {
        it('renders the singleFridgeLocationMarker', () => {
            // TODO: write this test
        });
        it('renders the singleFridgeLocationPopup', () => {
            // TODO: write this test
        });
    });
});