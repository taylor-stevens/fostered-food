/**
 * Tests for the SingleFridgeContactInfo.
 */
describe('SingleFridgeContactInfo', () => {
    describe('will always render', () => {
        it('the singleFridgeContactInfo', () => {
            // TODO: write this test
        });
    });
    describe('if the contact list is NOT populated', () => {
        it('the singleFridgeContactInfo', () => {
            // TODO: write this test
        });
        it('does not render any singleContactPoints', () => {
            // TODO: write this test
        });
        it('the singleFridgeContactInfo', () => {
            // TODO: write this test
        });
    });
    describe('if the contact list is populated', () => {
        it('the singleFridgeContactInfo', () => {
            // TODO: write this test
        });
        it('renders the same number of singleContactPoints as there are contact points', () => {
            // TODO: write this test
        });
        it('the singleFridgeContactInfo', () => {
            // TODO: write this test
        });
    });
});